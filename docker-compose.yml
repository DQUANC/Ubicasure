version: '3.8'
services:
  backend:
    build: ./back
    ports:
      - "3200:3200"
    env_file: ./back/.env

  mongodb:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
